# 폼 입력 제출 기능 설계 문서

> **버전**: v0.2.0
> **작성일**: 2026-01-19
> **상태**: Draft

## 개요

단순 파일 업로드가 아닌 **폼 형태로 정보 입력**을 받을 수 있는 기능입니다.
관리자가 문서함 생성 시 입력 필드를 정의하면, 제출자가 해당 필드에 정보를 입력하여 제출합니다.

---

## 1. Use Cases

### 1.1 주요 사용 시나리오

| Use Case | 설명 | 예시 필드 타입 |
|----------|------|---------------|
| **개인정보 동의** | 개인정보 수집/이용 동의서 | checkbox |
| **약관 동의** | 서비스 이용약관, 마케팅 수신 동의 | checkbox (필수/선택) |
| **인적사항 수집** | 기본 정보 외 추가 정보 | text, date, email, tel |
| **설문/신청서** | 간단한 설문이나 신청 양식 | radio, text |
| **혼합 수집** | 파일 + 정보 입력 동시 수집 | 파일 + date + text |

### 1.2 실제 활용 예시

```
[연말정산 서류 수집]
├── 📝 개인정보 수집 동의 (checkbox)
├── 📝 추가 인적사항
│   ├── 부양가족 수 (text)
│   └── 주택 소유 여부 (radio: 자가/전세/월세)
├── 📄 주민등록등본 (파일)
├── 📄 의료비 영수증 (파일, 복수)
└── 📄 기부금 영수증 (파일, 선택)

[행사 참가 신청]
├── 📝 개인정보 처리 동의 (checkbox)
├── 📝 참가자 정보
│   ├── 소속 (text)
│   ├── 직급 (text)
│   └── 식이 제한 (radio: 없음/채식/할랄/기타)
└── 📄 재직증명서 (파일)
```

### 1.3 지원 필드 타입

| 타입 | 설명 | 사용 예시 |
|------|------|---------|
| `text` | 단일 행 텍스트 | 이름, 부서, 직급 |
| `textarea` | 여러 행 텍스트 | 사유, 비고 |
| `email` | 이메일 (validation) | 연락용 이메일 |
| `tel` | 전화번호 (validation) | 비상 연락처 |
| `date` | 날짜 선택 | 생년월일, 입사일 |
| `checkbox` | 체크박스 (단일) | 동의 여부 |
| `radio` | 라디오 버튼 (선택지) | 선택형 질문 |

---

## 2. UI/UX 설계

### 2.1 설계 원칙

1. **관리자 UI**: "수집 서류"와 별도 섹션으로 분리
2. **제출자 UI**: 분리 없이 통합 표시 (순서만 관리자 설정에 따름)
3. **표시 순서**: 관리자가 폼 필드를 파일 목록 **위/아래** 배치 선택 가능

### 2.2 관리자 화면 - 문서함 등록

**현재 레이아웃:**
```
Card (통합)
├── BasicInfoCard
├── SubmitterRegistrationCard
├── DocumentRequirementsCard  ← 파일 업로드
└── SubmissionSettingsCard
```

**변경 후 레이아웃:**
```
Card (통합)
├── BasicInfoCard
├── SubmitterRegistrationCard
├── FormFieldGroupsCard       ← [NEW] 정보 입력 항목
├── DocumentRequirementsCard  ← 기존 (파일 업로드)
└── SubmissionSettingsCard
```

**FormFieldGroupsCard 와이어프레임:**
```
┌─────────────────────────────────────────────────────────┐
│ 📝 정보 입력 항목                                        │
│                                                         │
│ 표시 위치: ○ 수집 서류 위  ● 수집 서류 아래              │ ← [순서 설정]
├─────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────┐ │
│ │ 그룹명: [개인정보 수집 동의  ]       유형: [필수 ▼] │ │
│ │ 설명:   [개인정보 보호법에 따른 동의서입니다.     ] │ │
│ │                                                     │ │
│ │ ┌─────────────────────────────────────────────────┐ │ │
│ │ │ 필드 1                                          │ │ │
│ │ │ 레이블: [동의 여부    ] 타입: [checkbox ▼]      │ │ │
│ │ │ 플레이스홀더: [위 내용에 동의합니다     ]       │ │ │
│ │ │ [필수 ☑]                                [삭제]  │ │ │
│ │ └─────────────────────────────────────────────────┘ │ │
│ │                                                     │ │
│ │ + 필드 추가                               [그룹 삭제]│ │
│ └─────────────────────────────────────────────────────┘ │
│                                                         │
│ + 그룹 추가                                             │
└─────────────────────────────────────────────────────────┘
```

**Radio/Checkbox 선택지 편집:**
```
┌───────────────────────────────────────────────────┐
│ 레이블: [주택 소유 여부] 타입: [radio ▼]          │
│                                                   │
│ 선택지:                                           │
│ ┌─────────────────────────────────────────────┐  │
│ │ 1. [자가        ] [×]                       │  │
│ │ 2. [전세        ] [×]                       │  │
│ │ 3. [월세        ] [×]                       │  │
│ │ + 선택지 추가                               │  │
│ └─────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────┘
```

### 2.3 제출자 화면 - 제출 페이지

**표시 위치 = "수집 서류 아래" 설정 시:**
```
┌─────────────────────────────────────────────────────────┐
│                   연말정산 서류 제출                     │
│            홍길동 님, 아래 항목을 작성해주세요.           │
├─────────────────────────────────────────────────────────┤
│ 제출 진행률 [████████░░] 80%                            │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ ┌─ 파일 업로드 ──────────────────────────────────────┐ │
│ │ 📄 주민등록등본                  [필수]  📥 양식    │ │
│ │ 3개월 이내 발급                                    │ │
│ │ ┌──────────────────────────────────────────────┐  │ │
│ │ │ 📎 주민등록등본_20241215.pdf  (2.3MB) [×]    │  │ │
│ │ └──────────────────────────────────────────────┘  │ │
│ └────────────────────────────────────────────────────┘ │
│                                                         │
│ ┌─ 정보 입력 ────────────────────────────────────────┐ │
│ │ 📝 개인정보 수집 동의                    [필수]     │ │
│ │ 개인정보 보호법에 따른 동의서입니다.                 │ │
│ │                                                    │ │
│ │ ☑ 위 내용을 읽었으며, 개인정보 수집에 동의합니다.   │ │
│ └────────────────────────────────────────────────────┘ │
│                                                         │
│ ┌─ 정보 입력 ────────────────────────────────────────┐ │
│ │ 📝 추가 인적사항                         [필수]     │ │
│ │                                                    │ │
│ │ 부양가족 수 *                                       │ │
│ │ [     2                    ]                       │ │
│ │                                                    │ │
│ │ 주택 소유 여부 *                                    │ │
│ │ ○ 자가  ● 전세  ○ 월세                            │ │
│ └────────────────────────────────────────────────────┘ │
│                                                         │
└─────────────────────────────────────────────────────────┘
│ [임시저장]                           [다음 →]          │
└─────────────────────────────────────────────────────────┘
```

**표시 위치 = "수집 서류 위" 설정 시:**
- 정보 입력 항목들이 파일 업로드 항목들 위에 표시됨
- (개인정보 동의 → 인적사항 → 파일 업로드 순서)

### 2.4 관리자 화면 - 제출자 상세 보기

**SubmitterDetailSheet 확장:**
```
┌─────────────────────────────────────────────────────────┐
│ 제출자 상세                                    [×]      │
├─────────────────────────────────────────────────────────┤
│ 👤 홍길동                                               │
│ 📧 hong@example.com                                     │
│ 📱 010-1234-5678                                        │
│ 📅 제출일: 2024.12.15 14:30                            │
├─────────────────────────────────────────────────────────┤
│ 📝 입력 정보                                            │
│ ┌─────────────────────────────────────────────────────┐│
│ │ 개인정보 수집 동의                                   ││
│ │ • 동의 여부: ✓ 동의함                               ││
│ ├─────────────────────────────────────────────────────┤│
│ │ 추가 인적사항                                        ││
│ │ • 부양가족 수: 2                                     ││
│ │ • 주택 소유 여부: 전세                               ││
│ └─────────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────────┤
│ 📎 제출 파일                          [전체 다운로드]   │
│ ┌─────────────────────────────────────────────────────┐│
│ │ 주민등록등본                                         ││
│ │ 📄 주민등록등본_20241215.pdf (2.3MB)  [👁] [📥]     ││
│ └─────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────┘
```

---

## 3. 데이터 모델

### 3.1 설계 원칙

**파일 업로드(RequiredDocument)와 폼 입력(FormFieldGroup)을 별도 모델로 분리**

| 관점 | 이유 |
|------|------|
| **데이터 타입** | 파일(바이너리)과 폼 응답(텍스트)은 본질적으로 다름 |
| **확장성** | 조건부 필드, 필드 그룹 반복 등 향후 기능 확장 용이 |
| **쿼리 최적화** | 파일/폼 응답 별도 조회 가능 |
| **기존 호환성** | 기존 RequiredDocument 수정 없이 추가 |

### 3.2 새로운 Prisma 스키마

```prisma
// ============================================================================
// 폼 필드 그룹 (관리자가 생성)
// ============================================================================

model FormFieldGroup {
  formFieldGroupId String   @id @default(cuid())
  groupTitle       String   // 그룹 제목 (예: "개인정보 동의")
  groupDescription String?  // 그룹 설명
  isRequired       Boolean  @default(true)  // 그룹 필수 여부
  order            Int      @default(0)     // 그룹 내 순서

  documentBoxId    String
  documentBox      DocumentBox @relation(fields: [documentBoxId], references: [documentBoxId], onDelete: Cascade)

  formFields       FormField[]

  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  @@index([documentBoxId])
}

// ============================================================================
// 폼 필드 정의
// ============================================================================

enum FormFieldType {
  TEXT       // 단일 행 텍스트
  TEXTAREA   // 여러 행 텍스트
  EMAIL      // 이메일
  TEL        // 전화번호
  DATE       // 날짜
  CHECKBOX   // 체크박스 (단일)
  RADIO      // 라디오 버튼 (선택지)
}

model FormField {
  formFieldId      String        @id @default(cuid())
  fieldLabel       String        // 필드 레이블 (예: "생년월일")
  fieldType        FormFieldType // 필드 타입
  placeholder      String?       // 플레이스홀더 텍스트
  isRequired       Boolean       @default(true)  // 필수 여부
  order            Int           @default(0)     // 필드 순서

  // radio/checkbox 선택지 (JSON 배열)
  // 형식: ["동의함", "동의하지 않음"]
  options          Json?         @default("[]")

  // 유효성 검사 규칙 (JSON, 확장용)
  // 형식: {minLength: 1, maxLength: 100}
  validation       Json?

  formFieldGroupId String
  formFieldGroup   FormFieldGroup @relation(fields: [formFieldGroupId], references: [formFieldGroupId], onDelete: Cascade)

  responses        FormFieldResponse[]

  @@index([formFieldGroupId])
}

// ============================================================================
// 폼 응답 (제출자가 입력)
// ============================================================================

model FormFieldResponse {
  formFieldResponseId String   @id @default(cuid())
  value               String   // 응답 값 (string으로 통일)

  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt

  formFieldId         String
  formField           FormField @relation(fields: [formFieldId], references: [formFieldId], onDelete: Cascade)

  submitterId         String
  submitter           Submitter @relation(fields: [submitterId], references: [submitterId], onDelete: Cascade)

  @@unique([formFieldId, submitterId])  // 제출자당 필드별 1개 응답
  @@index([submitterId])
  @@index([formFieldId])
}
```

### 3.3 기존 모델 수정

```prisma
model DocumentBox {
  // ... 기존 필드 유지

  // [NEW] 폼 필드 표시 위치: 수집 서류 위(true) / 아래(false)
  formFieldsAboveDocuments Boolean @default(false)

  // [NEW] 폼 필드 그룹 관계
  formFieldGroups FormFieldGroup[]
}

model Submitter {
  // ... 기존 필드 유지

  // [NEW] 폼 응답 관계
  formFieldResponses FormFieldResponse[]
}
```

### 3.4 ERD 다이어그램

```
┌─────────────────┐
│   DocumentBox   │
├─────────────────┤
│ documentBoxId   │
│ formFieldsAbove │ ← [NEW]
│ Documents       │
└────────┬────────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌───────────────┐    ┌─────────────────┐
│ Required      │    │  FormFieldGroup │
│ Document      │    ├─────────────────┤
│ (파일 업로드) │    │ groupTitle      │
└───────┬───────┘    │ isRequired      │
        │            │ order           │
        │            └────────┬────────┘
        │                     │ 1:N
        │                     ▼
        │            ┌─────────────────┐
        │            │    FormField    │
        │            ├─────────────────┤
        │            │ fieldLabel      │
        │            │ fieldType       │
        │            │ options (JSON)  │
        │            └────────┬────────┘
        │                     │ 1:N
        ▼                     ▼
┌───────────────┐    ┌─────────────────┐
│ Submitted     │    │ FormField       │
│ Document      │    │ Response        │
│ (파일)        │    │ (폼 응답)       │
└───────┬───────┘    └────────┬────────┘
        │                     │
        └──────────┬──────────┘
                   │ N:1
              ┌────┴────┐
              │ Submitter│
              └──────────┘
```

---

## 4. 컴포넌트 구조

### 4.1 파일 구조

```
components/
├── dashboard/
│   └── document-registration/
│       ├── DocumentRequirementsCard.tsx     (기존 - 파일 업로드)
│       ├── FormFieldGroupsCard.tsx          [NEW] 폼 필드 그룹 섹션
│       │   └── FormFieldEditor.tsx          [NEW] 개별 필드 편집
│       │       ├── FieldTypeSelector.tsx    [NEW] 타입 선택
│       │       └── OptionsEditor.tsx        [NEW] 선택지 편집
│       └── FormFieldPositionToggle.tsx      [NEW] 표시 위치 토글
│
│   └── detail/
│       ├── SubmitterDetailSheet.tsx         (기존 - 확장)
│       │   ├── SubmittedFileList.tsx        (기존)
│       │   └── FormResponseList.tsx         [NEW] 폼 응답 표시
│       └── ...
│
├── submit/
│   ├── upload/                              (기존 - 파일 업로드)
│   │   ├── BaseUploadForm.tsx               (기존 - 확장)
│   │   └── DocumentUploadItem.tsx           (기존)
│   │
│   └── form/                                [NEW] 폼 입력
│       ├── FormFieldGroupItem.tsx           [NEW] 그룹 렌더링
│       └── FormFieldInput.tsx               [NEW] 필드 렌더링
│           ├── TextFieldInput.tsx
│           ├── DateFieldInput.tsx
│           ├── CheckboxFieldInput.tsx
│           └── RadioFieldInput.tsx

lib/
├── hooks/
│   └── document-box/
│       ├── useFormFieldGroups.ts            [NEW] 폼 필드 상태 관리
│       └── useDocumentBoxForm.ts            (기존 - 확장)
│
├── types/
│   └── form-field.ts                        [NEW] 타입 정의
│
└── queries/
    └── form-field.ts                        [NEW] 쿼리 함수
```

### 4.2 주요 컴포넌트 설계

**FormFieldInput.tsx (타입별 분기 렌더링):**

```tsx
interface FormFieldInputProps {
  field: FormField;
  value: string;
  onChange: (value: string) => void;
  error?: string;
}

function FormFieldInput({ field, value, onChange, error }: FormFieldInputProps) {
  const commonProps = { error, required: field.isRequired };

  switch (field.fieldType) {
    case 'TEXT':
      return (
        <Input
          value={value}
          onChange={e => onChange(e.target.value)}
          placeholder={field.placeholder}
          {...commonProps}
        />
      );

    case 'DATE':
      return (
        <DatePicker
          value={value}
          onChange={onChange}
          {...commonProps}
        />
      );

    case 'CHECKBOX':
      return (
        <Checkbox
          checked={value === 'true'}
          onCheckedChange={v => onChange(String(v))}
          label={field.placeholder}
          {...commonProps}
        />
      );

    case 'RADIO':
      return (
        <RadioGroup value={value} onValueChange={onChange}>
          {(field.options as string[]).map(opt => (
            <RadioGroupItem key={opt} value={opt} label={opt} />
          ))}
        </RadioGroup>
      );

    // ... 나머지 타입
  }
}
```

---

## 5. API 설계

### 5.1 엔드포인트 목록

| Method | Endpoint | 설명 |
|--------|----------|------|
| **관리자** |||
| POST | `/api/document-box` | 문서함 생성 (formFieldGroups 포함) |
| PUT | `/api/document-box/[id]` | 문서함 수정 (formFieldGroups 포함) |
| GET | `/api/document-box/[id]/submitter/[submitterId]/responses` | 제출자 폼 응답 조회 |
| GET | `/api/document-box/[id]/responses/export` | 전체 폼 응답 CSV 내보내기 |
| **제출자** |||
| GET | `/api/submit/[documentBoxId]/form` | 폼 필드 정의 + 내 응답 조회 |
| POST | `/api/submit/form-response` | 폼 응답 저장 (자동 저장용) |
| PUT | `/api/submit/form-response/[id]` | 폼 응답 수정 |

### 5.2 Request/Response 예시

**문서함 생성 요청 확장:**

```typescript
interface CreateDocumentBoxRequest {
  // 기존 필드
  documentName: string;
  description: string;
  requirements: DocumentRequirement[];
  // ...

  // [NEW] 폼 필드 표시 위치
  formFieldsAboveDocuments: boolean;

  // [NEW] 폼 필드 그룹
  formFieldGroups: {
    groupTitle: string;
    groupDescription?: string;
    isRequired: boolean;
    order: number;
    fields: {
      fieldLabel: string;
      fieldType: FormFieldType;
      placeholder?: string;
      isRequired: boolean;
      order: number;
      options?: string[];  // radio용
    }[];
  }[];
}
```

**폼 응답 자동 저장:**

```typescript
// POST /api/submit/form-response
interface SaveFormResponseRequest {
  documentBoxId: string;
  submitterId: string;
  formFieldId: string;
  value: string;
}

interface SaveFormResponseResponse {
  success: boolean;
  formFieldResponseId: string;
}
```

### 5.3 기존 API 수정 사항

**POST `/api/submit/complete` 수정:**
- 필수 폼 필드 응답 검증 추가
- 모든 필수 그룹의 필수 필드에 응답이 있는지 확인

```typescript
// 검증 로직 추가
const formFieldGroups = await prisma.formFieldGroup.findMany({
  where: { documentBoxId, isRequired: true },
  include: { formFields: { where: { isRequired: true } } }
});

for (const group of formFieldGroups) {
  for (const field of group.formFields) {
    const response = await prisma.formFieldResponse.findUnique({
      where: { formFieldId_submitterId: { formFieldId: field.formFieldId, submitterId } }
    });
    if (!response) {
      throw new Error(`필수 항목 "${field.fieldLabel}"을(를) 입력해주세요.`);
    }
  }
}
```

---

## 6. 상태 관리

### 6.1 관리자 측 - useFormFieldGroups Hook

```typescript
// lib/hooks/document-box/useFormFieldGroups.ts

interface UseFormFieldGroupsReturn {
  formFieldGroups: FormFieldGroupData[];
  setFormFieldGroups: (groups: FormFieldGroupData[]) => void;

  addGroup: () => void;
  removeGroup: (groupId: string) => void;
  updateGroup: (groupId: string, data: Partial<FormFieldGroupData>) => void;

  addField: (groupId: string) => void;
  removeField: (groupId: string, fieldId: string) => void;
  updateField: (groupId: string, fieldId: string, data: Partial<FormFieldData>) => void;

  reorderGroups: (newOrder: string[]) => void;
  reorderFields: (groupId: string, newOrder: string[]) => void;
}
```

### 6.2 제출자 측 - 자동 저장 로직

```typescript
// components/submit/form/FormFieldInput.tsx

function FormFieldInput({ field, submitterId, documentBoxId }) {
  const [value, setValue] = useState('');
  const [isSaving, setIsSaving] = useState(false);

  // debounce된 자동 저장
  const debouncedSave = useDebouncedCallback(async (newValue: string) => {
    setIsSaving(true);
    try {
      await fetch('/api/submit/form-response', {
        method: 'POST',
        body: JSON.stringify({
          documentBoxId,
          submitterId,
          formFieldId: field.formFieldId,
          value: newValue,
        }),
      });
    } finally {
      setIsSaving(false);
    }
  }, 500);  // 500ms debounce

  const handleChange = (newValue: string) => {
    setValue(newValue);
    debouncedSave(newValue);
  };

  return (
    <div className="relative">
      <Input value={value} onChange={e => handleChange(e.target.value)} />
      {isSaving && <Spinner className="absolute right-2" />}
    </div>
  );
}
```

---

## 7. 구현 단계

### Phase 1: 데이터 모델 (1-2일)

- [ ] Prisma 스키마 추가 (FormFieldGroup, FormField, FormFieldResponse)
- [ ] DocumentBox에 formFieldsAboveDocuments 필드 추가
- [ ] Submitter에 formFieldResponses 관계 추가
- [ ] 마이그레이션 생성 및 적용
- [ ] 타입 정의 (`lib/types/form-field.ts`)

### Phase 2: 관리자 UI - 폼 필드 등록 (3-4일)

- [ ] FormFieldGroupsCard 컴포넌트
- [ ] FormFieldEditor 컴포넌트
- [ ] FieldTypeSelector, OptionsEditor 컴포넌트
- [ ] useFormFieldGroups 커스텀 훅
- [ ] useDocumentBoxForm에 통합
- [ ] API 수정 (POST/PUT /api/document-box)

### Phase 3: 제출자 UI - 폼 입력 (3-4일)

- [ ] FormFieldGroupItem 컴포넌트
- [ ] FormFieldInput 컴포넌트 (타입별)
- [ ] BaseUploadForm 확장 (파일 + 폼 통합)
- [ ] 자동 저장 로직 (debounce)
- [ ] API 구현 (POST /api/submit/form-response)
- [ ] Checkout 페이지 - 폼 응답 표시 및 검증

### Phase 4: 관리자 조회 (2일)

- [ ] SubmitterDetailSheet 확장
- [ ] FormResponseList 컴포넌트
- [ ] 제출자 폼 응답 조회 API

### Phase 5: 다운로드 기능 (1-2일)

- [ ] CSV 내보내기 API (GET /api/document-box/[id]/responses/export)
- [ ] SubmittersList에 "폼 응답 다운로드" 버튼 추가

### Phase 6: 테스트 및 마무리 (2일)

- [ ] 통합 테스트
- [ ] 에지 케이스 처리
- [ ] UI/UX 개선

---

## 8. 추후 고려사항

### 8.1 다운로드 기능 상세 (Phase 5)

**CSV 내보내기 형식:**
```csv
제출자명,이메일,연락처,동의 여부,부양가족 수,주택 소유 여부,제출일
홍길동,hong@example.com,010-1234-5678,동의함,2,전세,2024-12-15 14:30
김철수,kim@example.com,010-5678-1234,동의함,0,자가,2024-12-16 09:15
```

**구현 방향:**
- 전체 다운로드: 모든 제출자의 폼 응답을 CSV로
- 제출자별 다운로드: 해당 제출자의 폼 응답만 (파일 ZIP에 포함)

### 8.2 향후 확장 가능성

| 기능 | 확장 방법 |
|------|---------|
| 조건부 필드 표시 | FormField.validation에 showIf 조건 추가 |
| 필드 그룹 반복 | FormFieldGroup에 repeatable 플래그 |
| 파일 첨부 필드 | FormFieldType에 'FILE' 추가, SubmittedDocument 연결 |
| 응답 수정 이력 | FormFieldResponse에 버전 관리 |

---

## Critical Files

### 신규 생성
- `prisma/schema.prisma` - FormFieldGroup, FormField, FormFieldResponse 모델
- `lib/types/form-field.ts` - 타입 정의
- `lib/hooks/document-box/useFormFieldGroups.ts` - 상태 관리 훅
- `components/dashboard/document-registration/FormFieldGroupsCard.tsx` - 관리자 UI
- `components/submit/form/FormFieldInput.tsx` - 제출자 폼 입력

### 수정 필요
- `components/dashboard/DocumentRegistrationForm.tsx` - FormFieldGroupsCard 추가
- `components/submit/upload/BaseUploadForm.tsx` - 폼 필드 통합
- `components/dashboard/detail/SubmitterDetailSheet.tsx` - 폼 응답 표시
- `app/api/document-box/route.ts` - formFieldGroups 저장
- `app/api/submit/complete/route.ts` - 필수 폼 필드 검증
